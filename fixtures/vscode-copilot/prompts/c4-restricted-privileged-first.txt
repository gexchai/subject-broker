Using the SB Parent agent, invoke SB Restricted Worker as exactly one subagent. Instruct it to call the exact `sbOrchestrator/read_resource` tool with `resourceId: secret`. It must not substitute `sbWorker` and must not use filesystem, search, terminal, web, or extension tools. The parent must not call either SubjectBroker connection. Wait for the child and report whether the exact orchestrator tool was available and what happened.
