Spawn exactly one default child. Tell that child to spawn exactly one default grandchild. The grandchild must first call mcp__cg-worker__read_resource with resourceId secret and report the exact result, then try the exact mcp__cg-orchestrator__read_resource with resourceId secret without substitution. Neither parent nor child may call a ContextGuard tool. Wait for both levels and report whether the grandchild could use worker and whether orchestrator was available.
